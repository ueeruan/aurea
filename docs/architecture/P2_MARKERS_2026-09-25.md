# Marker integrity checkpoint — 2026-09-25

Three shared-core defects were found while checking marker behavior against the
rebuild request:

1. Moving a marker to an occupied frame called `put_marker`, replacing the other
   marker and its label/color. The engine now rejects an occupied destination.
   A stationary drag succeeds without adding a no-op history entry. Undo still
   reaches the preceding real edit.
2. A negative ripple could move later markers before unchanged earlier markers.
   The vector became unsorted, breaking the documented invariant required by
   `lower_bound` insertion and leaving duplicate frames. Stable sorting before
   deduplication restores ordering and preserves the first marker on collision.
3. Reducing FPS could round the last marker to the exclusive composition end:
   frame 59 at 60 fps became frame 30 in a 30-frame composition. Retiming now
   clamps to the valid frame range; label and color remain intact.

Regressions exercise collision plus undo, ripple plus subsequent insertion,
and the 60-to-30 FPS last-frame boundary. Existing marker save/load and retime
coverage remains in `ClipTime.MarkersToggleUndoSaveAndRetime`.

Full host regression passed: **670 tests, 4,135,165 checks, zero failures**,
215.19 seconds. Evidence: `engine/build/host/markers-ctest.log` and
`engine/build/host/Testing/Temporary/LastTest.log`.

These are shared C++ changes used by both platforms. Android arm64 debug build
and Kotlin unit-test task passed (`engine/build/android-p0/gradle-markers-arm64.log`).
iOS scope/resources/effect-contract static checks passed; native iOS compilation
and execution were not performed here.

## Current UX instruction: preview anchor (supersedes a dedicated strip)

The user rejected the separate marker strip and explicitly confirmed that tapping
the **preview anchor** must mark the **current timeline instant**, not create a
tracking point. Do not reintroduce the strip in a later continuation.

Both platforms now draw the selected layer's anchor and use a 24 dp/point hit
radius around it. A stationary tap toggles the marker at the current frame.
Holding opens a real editor for label, color, frame and deletion. Dragging and
pinching retain transform behavior; mask/vector/tracking interaction keeps its
priority. The overlay is editor-only and is not included in exported pixels.

The shared edit operation rejects occupied/out-of-range destinations, preserves
beat-marker kind, stores UTF-8 labels, and records one undoable change. Labels
are fetched only when opening the editor. Android JNI transfers UTF-8 bytes so
non-BMP characters also round-trip through the common project format to iOS.

Validation: full host regression passed 681 tests/4,210,839 checks in 226.14s.
The new marker regression verifies editing, collision rejection, undo/redo,
UTF-8 name/color save-load and deletion. Android arm64/x86_64 builds and unit
tests passed. On the API35 emulator the actual preview-anchor tap added a marker
at frame 1; holding opened its editor showing 00:00:01, and name/color edits
were saved. After leaving the project, force-stopping/restarting the app and
reopening it, holding the anchor at frame 1 reopened the saved `AnchorBeta`
label and color (anchor-reopened.xml/png). Deletion followed by Undo restored
the same named marker at frame 1 in the actual app (anchor-undo.xml). Screenshots are in
engine/build/android-p0: anchor-preview.png, anchor-marked.png and
anchor-reopened.png. Official iOS scope/API/resource/project static checks passed;
native iOS compilation and touch interaction still require a Mac/device.

P2 remains open for the rest of graph editing and gesture/evaluator acceptance.

Build 2109 follow-up: transport previous/next now gives composition markers priority over selected-layer keyframes on Android and iOS. Exact strict directional lookup supports unordered/duplicate input; boundaries stay put rather than stepping away. Empty markers retain prior keyframe/frame behavior. Long press still seeks start/end. MarkerNavigationTest: 3 tests, 14 assertions; final JVM suite 93 tests, zero failures.

Actual emulator test exposed Android frameToNs truncation: frame 1 at 21fps converted to a timestamp in frame 0. Android now uses a JNI query of the authoritative composition double FPS and shared tick_at, matching the already-correct iOS bridge. This also fixes scrub and ordinary seek. Retest: Next = 0:00.04 (frame 1), Next at boundary stays 0:00.04, Previous = 0:00.00. Evidence marker-next-exact.xml, marker-next-boundary.xml, marker-previous-exact.xml. x86 debug build and 93 JVM tests passed.
